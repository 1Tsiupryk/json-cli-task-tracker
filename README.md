# json-cli-task-tracker
A simple command-line Task Tracker app where users can add, view, delete tasks and mark their status. Tasks are stored in a JSON file, so the task list persists between program runs. 
